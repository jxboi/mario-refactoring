import{describe,expect,it}from"vitest";import{newProject,newTask,newWorkspace}from"./store";import{parseWorkspaceJson,projectToJson,workspaceToJson}from"./export";
describe("workspace exports",()=>{it("round trips nested projects, grouped categories, and tasks",()=>{const p={...newProject("Launch"),tasks:[newTask()]},w={...newWorkspace("Team"),projects:[p]};const parsed=parseWorkspaceJson(workspaceToJson(w));expect(parsed.workspace?.projects[0].tasks).toHaveLength(1);expect(parsed.workspace?.categoryGroups.map(g=>g.id)).toEqual(["planning","work","general"])});it("rejects v4 and older exports",()=>{expect(parseWorkspaceJson(JSON.stringify({kind:"chisel-workspace",version:4,workspace:{}})).error).toContain("legacy")});it("does not export a project category",()=>{const json=JSON.parse(projectToJson(newProject("Launch"),newWorkspace().categories));expect(json.version).toBe(2);expect(json.project).not.toHaveProperty("category")})});
