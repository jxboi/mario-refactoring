import{describe,expect,it}from"vitest";import{parseTaskJson}from"./parse";describe("task import",()=>{it("parses task arrays",()=>expect(parseTaskJson('[{"title":"Ship"}]').items[0].title).toBe("Ship"));it("rejects typed legacy documents",()=>expect(parseTaskJson('{"type":"plan","items":[]}').errors).not.toHaveLength(0))});
