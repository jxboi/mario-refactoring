import type{CategoryDef,Task}from"../types";import{uid}from"../types";
export interface ParseResult{items:Task[];categories:CategoryDef[];errors:string[]}
export function parseTaskJson(text:string):ParseResult{try{const raw=JSON.parse(text),list=Array.isArray(raw)?raw:raw?.tasks;if(!Array.isArray(list))return{items:[],categories:[],errors:["Expected a task array."]};const now=Date.now();return{items:list.filter((x:any)=>typeof x?.title==="string").map((x:any)=>({id:uid(),title:x.title,description:typeof x.description==="string"?x.description:"",risk:["low","medium","high"].includes(x.priority)?x.priority:"medium",effort:["low","medium","high","xhigh"].includes(x.effort)?x.effort:"medium",category:typeof x.category==="string"?x.category:"other",tags:Array.isArray(x.tags)?x.tags:[],stage:["queued","active","reviewing","deployed","deferred"].includes(x.status)?x.status:"queued",blocked:false,blockReason:"",notes:[],createdAt:now,updatedAt:now})),categories:Array.isArray(raw?.categories)?raw.categories:[],errors:[]}}catch(e){return{items:[],categories:[],errors:[e instanceof Error?e.message:"Invalid JSON"]}}}
